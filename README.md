# MacroLens

MacroLens is a U.S. macroeconomic data analytics platform for organizing economic data, validating it, transforming it, and eventually producing macro-state insights for analysis and decision support.

The project is currently in early development. This repository contains the initial scaffold only; data ingestion, database models, API endpoints, analytics, frontend, and AI features will be added incrementally.

Planned pipeline:

```text
Raw -> Validate -> Process -> Analytics -> Macro State -> AI -> UI
```

The initial milestone is a Labor Market module using the PAYEMS, UNRATE, and ICSA economic series.
