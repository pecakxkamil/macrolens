# Architecture

MacroLens is planned around a staged macroeconomic data pipeline:

```text
Raw -> Validate -> Process -> Analytics -> Macro State -> AI -> UI
```

At this stage, the repository only defines the initial package and documentation structure. Architectural details will be added as each layer is designed and implemented.
